# Step 4 着手可否チェック（2026-03-11）

更新日: 2026-03-11（A-0 対応反映）

## 結論（更新）

- **Step 4 の実装着手は「可能（GO）」**。
- かつ、Sprint A 前提として定義していた **A-0a〜A-0c + `age_mode_override` 列追加は実装済み**。
- よって現時点は、**4-7（年齢適応UI）の本実装へ進んで問題ない状態**。

---

## 1) 現状（コードベース）

### 1-1. Step 3.9 で前倒しされたミッション自由度拡張は反映済み

- `child_missions` に `duration_days` / `target_value` を追加し、`set_child_mission` RPC も拡張済み。
- 進捗取得 RPC でも `duration_days` を返却しており、Step 3.9 の拡張方針と整合。
- UI 側でも期間（7/14日）・目標値カスタム・親期待パターンを扱う実装が入っている。

→ Step 4 前に行うとされた「ミッション運用の自由度拡張」は、主要要素が実装済みと判断できる。

### 1-2. AI 実装の最低限の土台は存在

- `@anthropic-ai/sdk` が依存関係にあり、共通クライアント `lib/ai/client.ts` も存在。
- 環境変数スキーマでも `ANTHROPIC_API_KEY` を必須化済み。

→ Step 4 の API 実装に進むためのベースは準備されている。

### 1-3. 4-7（年齢適応UI）の前提タスク A-0 は充足

Step 4 詳細設計で Sprint A 着手前タスクとして定義された項目は以下の通り実装済み:

- **A-0a**: `/settings/children` の「生年推奨入力」明示
  - 「生年（任意・入力推奨）」への文言更新と、年齢適応UIの説明文を追加済み。
- **A-0b**: `/settings/children/[id]/edit` の新設
  - 子どもの表示名・生年・年齢モードを編集できるページ/フォームを追加済み。
- **A-0c**: `updateChild` Server Action 追加
  - バリデーション、家族スコープの権限確認、更新処理、関連ページの revalidate を実装済み。
- **DB**: `children.age_mode_override` 列追加
  - `auto / junior / standard` の制約付きでマイグレーションを追加済み。

→ 4-7 の本実装に進む前提条件は満たされた。

---

## 2) フェーズ計画との照合

- フェーズ計画 v2 では Step 3.8 / 3.9 は完了扱いで、次アクションは **Step 4 着手（最優先）**。
- 同計画上、4-2/4-3 は Step 3.5 と Step 3.8 の導線整備を前提としており、ここは計画上クリア。

→ 計画レベルでも Step 4 を進めてよい状態。

---

## 3) Step 4 設計提案との整合

- Step 4 詳細設計は、実装順を **Sprint A (4-7) → Sprint B (4-8) → Sprint C (4-2)** と定義。
- Sprint A の前提（A-0a〜A-0c）は実装済みのため、順序どおり次に 4-7 へ着手可能。
- 本ドキュメントでも、実装順は **4-7 → 4-8 → 4-2** で統一する。

---

## 4) 最終判断（実行判断）

- **判断: Step 4 実装を継続して問題なし（GO）**。
- **次の推奨着手順**:
  1. 4-7 年齢適応UI（AgeModeProvider / 文言モード連携）
  2. 4-8 2層ジャンル
  3. 4-2 橋渡しレコメンド（最優先KPI検証）
  4. 4-1 / 4-3 / 4-6 / 4-4 を段階的に展開
