# Step 4 着手可否チェック（2026-03-11）

## 結論

- **Step 4 の実装着手は「可能（GO）」**。
- ただし、Step 4 詳細設計で Sprint A 前提として明記されている **A-0a〜A-0c を最初の実装スコープとして先行完了**する前提。
- したがって実務上は、**Step 4 開始 = まず A-0 を実装する** 進め方が妥当。

---

## 1) 現状（コードベース）

### 1-1. Step 3.9 で前倒しされたミッション自由度拡張は反映済み

- `child_missions` に `duration_days` / `target_value` を追加し、`set_child_mission` RPC も拡張済み。
- 進捗取得 RPC でも `duration_days` を返却しており、Step 3.9 の拡張方針と整合。
- UI 側でも期間（7/14日）・目標値カスタム・親期待パターンを扱う実装が入っている。

→ Step 4 前に行うとされた「ミッション運用の自由度拡張」は、主要要素が実装済みと判断できる。

### 1-2. AI 実装の最低限の土台は存在

- `@anthropic-ai/sdk` が依存関係にあり、共通クライアント `lib/ai/client.ts` も存在。
- 環境変数スキーマでも `ANTHROPIC_API_KEY` を必須化済み。

→ Step 4 の API 実装に進むためのベースは準備されている。

### 1-3. 4-7（年齢適応UI）の前提タスクは未充足

Step 4 詳細設計で Sprint A 着手前タスクとして定義された以下は、現状コード上は未実装:

- **A-0a**: `/settings/children` の「生年推奨入力」明示
  - 現状は「生年（任意）」のみで、年齢適応UIのために推奨という説明がない。
- **A-0b**: `/settings/children/[id]/edit` の新設
  - 該当ページは未確認（未実装）。
- **A-0c**: `updateChild` Server Action 追加
  - `app/actions/family.ts` には `createChild` はあるが `updateChild` は未定義。
- 併せて、設計で想定している `children.age_mode_override` 列追加も未実装。

→ 4-7 の本実装に直行するには前提不足。A-0 を最初に実装する必要がある。

---

## 2) フェーズ計画との照合

- フェーズ計画 v2 では Step 3.8 / 3.9 は完了扱いで、次アクションは **Step 4 着手（最優先）**。
- 同計画上、4-2/4-3 は Step 3.5 と Step 3.8 の導線整備を前提としており、ここは計画上クリア。

→ 計画レベルでは Step 4 に進んで良い状態。

---

## 3) Step 4 設計提案との整合

- Step 4 詳細設計は、実装順を **Sprint A (4-7) → Sprint B (4-8) → Sprint C (4-2)** と定義。
- さらに Sprint A の前提として A-0a〜A-0c を明示。

→ 設計どおり進めるなら、Step 4 開始時点で最初に A-0 を完了させるのが必須。

---

## 4) 最終判断（実行判断）

- **判断: Step 4 実装に入って問題なし（GO）**。
- **開始条件**: Sprint A の先頭に A-0a〜A-0c（+ `age_mode_override` マイグレーション）を組み込む。
- **推奨着手順**:
  1. A-0a〜A-0c + DB列追加
  2. 4-7 年齢適応UI
  3. 4-8 2層ジャンル
  4. 4-2 橋渡しレコメンド（最優先KPI検証）
